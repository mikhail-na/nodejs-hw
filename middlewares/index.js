const validateFunc = require("./validateFunc");
const handleBodyChange = require("./handleBodyChange");
const isValidIdFunc = require("./isValidIdFunc");
const authenticate = require("./authenticate");
const upload = require("./upload");

module.exports = {
  validateFunc,
  handleBodyChange,
  isValidIdFunc,
  authenticate,
  upload,
};